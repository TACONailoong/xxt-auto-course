# 更新日志

## 1.11.0

### 新增
- 检测到人脸/安全验证弹窗时自动暂停（不绕过，需手动完成）
- 扩展更新后提示刷新课程页
- 页面进入后台时提示可能被浏览器限速

### 修复
- 暂停/限流后 iframe 进度同步不再覆盖暂停原因
- 存储短暂失败不再直接永久停表（仅扩展上下文失效时硬停止）
- 会话统计展示改为活跃学习时长；切章统计不再冲掉 activeMs

### 改进
- 用视频 `timeupdate` / 页面可见性变化驱动恢复检查，降低后台标签限速影响
- 学完暂停使用 `done` 状态相位，与限流区分

## 1.10.0

### 修复
- iframe 内视频进度/状态同步到顶层浮层与弹窗（此前进度常停留在 0%）
- 自动暂停后浮层保留限流/学完原因，不再被“已停止”覆盖
- 会话时长上限改为按活跃学习时间累计，暂停期间不计时
- 目录切章支持回绕查找更早的未完成小节
- 空目录提示不再虚高“剩余未完成”数量

### 改进
- 视频卡顿分级恢复：点击播放 → 静音重试 → 微调进度 → reload
- 自动暂停时避免重复 toast
- 设置导入强制布尔/数字类型
- 弹窗提示建议保持课程页前台，避免后台标签被限速

## 1.9.0

### 新增
- 学完自动暂停：目录无剩余未完成章节时自动停止学习
- 会话限流：可设置本会话最多切章数 / 最大学习分钟数，达到后自动暂停
- `npm run pack`：一键打包为可加载的 zip 扩展包（输出到 `dist/`）

### 改进
- 答题指纹集合增加上限，避免长期运行内存增长
- 设置摘要芯片显示限流与学完即停状态
- 补齐限流相关单元测试

## 1.8.0

### 新增
- 快捷键：`Alt+Shift+S` 启停，`Alt+Shift+H` 显隐浮层
- 设置导入 / 导出（JSON）
- 目录剩余未完成章节数统计与展示
- 页面内 toast 反馈（启停、切章、限流等）
- 答题指纹去重，避免重复计分

### 改进
- 会话统计与日志体验打磨

## 1.7.0

### 新增
- 答题仅计首次；会话时长显示
- 选项摘要芯片；倍速 >2x 提示
- 恢复默认设置；浮层双击复位位置

## 1.6.0

### 新增
- 浮层拖拽 / 收起 / 快捷调速
- 会话统计（切章 / 答题）

## 1.5.0

### 改进
- 弹窗 UI 重做：品牌头区、主开关、折叠「更多选项」

## 1.4.0 及更早

- 页签切换、静音、测验跳过、目录切章、浮层、防挂机、日志、badge
- Manifest V3 基础可用版本
